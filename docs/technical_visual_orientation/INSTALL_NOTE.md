# Install note

Copy the entire `docs/technical_visual_orientation/` folder into the root of `a10-cosmology-137-gated`.

Then open:

```text
docs/technical_visual_orientation/index.html
```

This is a standalone browser file. No package installation is needed.
