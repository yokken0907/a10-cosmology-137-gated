# CLASS Patch / Overlay Status

The uploaded reproducibility archive contains modified CLASS overlay files for:

- `source/background.c`
- `source/input.c`
- `source/perturbations.c`
- `include/background.h`
- `include/input.h`
- `include/perturbations.h`

This GitHub-ready folder does **not** include the full modified CLASS source overlay, because a clean patch/diff against a specified upstream CLASS version should be prepared and reviewed before public redistribution.

For a complete public release, prepare:

1. upstream CLASS version / commit;
2. `a10_unified_patch.diff`;
3. patch application instructions;
4. license/provenance notice.
